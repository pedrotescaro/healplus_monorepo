# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
  # You can store secrets in Cloud Secret Manager and expose them as environment
  # variables. Add a new secret version and then uncomment the lines below.
  # https://firebase.google.com/docs/app-hosting/configure#secrets
  secrets:
    - secret: NEXT_PUBLIC_FIREBASE_API_KEY
    - secret: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    - secret: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    - secret: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    - secret: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    - secret: NEXT_PUBLIC_FIREBASE_APP_ID
    - secret: GEMINI_API_KEY
  # Node.js version for the runtime
  nodeVersion: "20"
  # Build command
  buildCommand: "npm run build"
  # Start command
  startCommand: "npm start"

